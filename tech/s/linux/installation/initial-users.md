## By default, Arch only creates:
- A `root` account (with full privileges)
- `sudo`
  - `sudo` has to be installed manually.
    ```sh
    pacman -S sudo
    ```
  - `sudo` can be prefixed only in non-root users.

##
